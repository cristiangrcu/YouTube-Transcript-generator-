# Nur für lokale Entwicklung nötig, wenn du das Python-Backend separat auf Port 8000 laufen lässt.
# Auf Vercel bleibt die Variable ungesetzt — dann werden relative /api-URLs benutzt.
# NEXT_PUBLIC_API_URL=http://localhost:8000
